function HeaderOption({ Icon , title , selected }) {
    return (
        <div className={`{flex}`}>
        <Icon className = "h-4" />
            
        </div>
    )
}

export default HeaderOption;
